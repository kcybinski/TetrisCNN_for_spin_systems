[pytest]
testpaths = tests
markers =
    data: tests that require the data/ directory (skip cleanly if absent)
    slow: tests that are slow (e.g. run real training loops)
    accelerator: tests that need a non-CPU device (cuda/mps); skip cleanly on CPU-only machines
addopts = -q
